{% set layouts = group.layouts(postId) %}

{% if layouts is not empty %}
    <div class="acfbreeze--content-group">
        {% for layout in layouts %}
            {% include layout.path() with {
                layout: layout,
                package: package
            } %}
        {% endfor %}
    </div>
{% endif %}