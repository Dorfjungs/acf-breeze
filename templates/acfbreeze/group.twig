{% if id is defined %}
    {% for package in packages %}
        {% for group in package.groups %}
            {% if group.name == name and group.render %}
                {% include group.path() with {
                    postId: id,
                    group: group,
                    package: package
                } %}
            {% endif %}
        {% endfor %}
    {% endfor %}
{% endif %}